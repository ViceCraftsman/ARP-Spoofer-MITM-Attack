# API Reference

This module exposes the following public service interfaces:

- `ILabTool` - orchestrates simulation and aggregation logic.
- `IDataProvider` - abstraction for data sources.
- `IRepository` - in-memory storage of results and snapshots.

For internal usage, see the unit tests and `Program.cs`.
